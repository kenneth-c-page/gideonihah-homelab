all:
  children:
    myhosts:
      hosts:
        immich-headscale:
          ansible_host: 100.64.0.3 
      vars:
        ansible_user: ubuntu 
        ansible_ssh_private_key_file: ~/.ssh/pre-homelab-immich-headscale.pem